import { Content } from '../../components/Content';

// `app/page.tsx` is the UI for the `/` URL
export default function Page() {
  return (
    <div className="text-zinc-100 h-screen">
      <Content />
    </div>
  );
}